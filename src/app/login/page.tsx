import { MockPage } from "@/components/MockPage";

export default function LoginPage() {
  return (
    <MockPage
      title="Log In"
      description="This mock login page will be replaced when Clerk authentication is added."
    />
  );
}

