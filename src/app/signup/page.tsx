import { MockPage } from "@/components/MockPage";

export default function SignupPage() {
  return (
    <MockPage
      title="Sign Up"
      description="This mock signup page will be replaced when Clerk authentication is added."
    />
  );
}
